#include <bits/stdc++.h>
using namespace std;
int main() {
	ios::sync_with_stdio(false); cin.tie(NULL);
//	freopen("input.txt", "r", stdin);
	int n;
	cin >> n;
	while(n--){
		int city_n, pilot_n;
		cin >> city_n >> pilot_n;
		cout << city_n-1 << endl;
		while (pilot_n--){
			int a, b; cin >> a >> b;
		}
	}
	return 0;
}

